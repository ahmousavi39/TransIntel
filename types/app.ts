/**
 * Type definitions for your app
 */

export interface User {
  id: string;
  name: string;
  email: string;
  // Add your user properties
}

export interface AppSettings {
  notifications: boolean;
  language: string;
  // Add your app settings
}

// Add more type definitions as needed
